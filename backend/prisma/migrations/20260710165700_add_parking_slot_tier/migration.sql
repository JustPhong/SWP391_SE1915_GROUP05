BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ParkingSlot] ADD [tier] NVARCHAR(30) NOT NULL CONSTRAINT [ParkingSlot_tier_df] DEFAULT 'REGULAR';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
